spring:
  application:
    name=sever_eureka
server:
  port: 8761
eureka:
  client:
      reister-with-eureka: false
      fetch-registry: false
logging:
     level:
      com.netflix.eureka: OFF
      com.netflix.discovery: OFF